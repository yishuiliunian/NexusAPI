# pnpm + Bazel rules_js 要求的配置
# - hoist=false 防止 hoist 打破 Bazel 的 isolated node_modules
# - public-hoist-pattern 清空，避免隐式 hoisting
hoist=false
public-hoist-pattern[]=""

# 忽略 peer 依赖解析错误，避免工作区升级被阻断
strict-peer-dependencies=false
auto-install-peers=true

# 包管理器锁文件版本
lockfile-version=9
